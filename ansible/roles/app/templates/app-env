APP_ENV={{ app_env }}

# Ignored in production
HTTP_PORT=9090

TRIAL_DAY_COUNT=15

FONT_PATH=/fonts

SITE_URL={{ site_url }}
SITE_DOMAIN={{ site_domain }}

REGISTER_URL={{ register_url }}
ENCRYPTION_KEY={{ encryption_key }}
CACHE_DIR=/cache

# Database
DB_HOST=tcp({{ app_db_host }}:3306)
DB_USERNAME={{ app_db_username }}
DB_PASSWORD={{ app_db_password }}
DB_DATABASE={{ app_db_database }}

# Redis
REDIS_HOST=redis.skyclerk.com:6379

# Object store
OBJECT_REGION={{ object_region }}
OBJECT_BUCKET={{ object_bucket }}
OBJECT_ACCESS_KEY_ID={{ object_access_key }}
OBJECT_SECRET_ACCESS_KEY={{ object_secret_key }}
OBJECT_ENDPOINT={{ object_endpoint }}
OBJECT_BASE_URL={{ object_base_url }}

# Stripe
STRIPE_MONTHLY_PLAN={{ stripe_monthly_plan }}
STRIPE_YEARLY_PLAN={{ stripe_yearly_plan }}
STRIPE_SECRET_KEY={{ stripe_secret_key }}
# STRIPE_SIGNING_SECRET=

# Email
MAIL_DRIVER=postmark
MAIL_HOST=127.0.0.1
MAIL_PORT=1025
MAIL_USERNAME=
MAIL_PASSWORD=

# Mailgun
MAILGUN_DOMAIN={{ mailgun_domain }}
MAILGUN_API_KEY={{ mailgun_api_key }}

# Postmark
POSTMARK_SERVER_KEY={{ postmark_server_key }}
POSTMARK_ACCOUNT_KEY={{ postmark_account_key }}

AWS_CLOUDFRONT_KEY_ID={{ aws_cloudfront_key_id }}
AWS_CLOUDFRONT_PRIVATE_SIGN_KEY={{ aws_cloudfront_private_sign_key }}

IMAGINARY_HOST={{ imaginary_host }}
IMAGINARY_KEY={{ imaginary_api_key }}

SLACK_HOOK={{ slack_hook }}

# Sendy
SENDY_API_KEY={{ sendy_api_key }}
SENDY_TRIAL_LIST={{ sendy_trial_list }}
SENDY_EXPIRED_LIST={{ sendy_expired_list }}
SENDY_SUBSCRIBE_LIST={{ sendy_subscribe_list }}

HTTP_LOG_REQUESTS=true
